# Gmail SMTP Configuration
# Get your App Password from: https://myaccount.google.com/apppasswords
# IMPORTANT: Remove spaces from the password (Google shows it with spaces for readability)
GMAIL_USERNAME=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password-without-spaces

# To use in development, run: ./dev.sh
# Or manually: export $(cat .env | grep -v '^#' | xargs) && iex -S mix phx.server

